# AgentSociety² 参赛研究计划：农业政策模拟与"政策沙盒"（v2）

> 赛道：数智公共管理与国家治理（AI 社会科学家研究挑战赛）
> 方向：农林经济管理（农业政策评估、农户行为、资源与环境经济学）
> 版本：v2（2026-08-20，基于初筛材料与前沿文献更新）

---

## 0. 一句话定位

把"农户"建模为带真实偏好、风险态度与家庭约束的 LLM 智能体，放在一个可标定的村庄农业经济环境里，逐个施加不同涉农政策冲击，通过可重复、可干预的反事实模拟，估计政策对种植结构、农户收入与粮食安全的影响，形成"政策实施前的沙盒评估"。

## 1. 为什么做这件事

### 1.1 政策评估的现实缺口

农业政策正从增产导向转向保供给、促增收、可持续的多重目标。粮食直补、农业保险保费补贴、农机购置补贴、土地流转激励、生态补偿等工具都在用，但政策落地前缺少低成本、可迭代的效应评估手段。

传统方法有两条路，各有限制：

- 事后计量：政策已经落地，只能观测已发生的结果，做不了反事实对照。农户是否改变种植结构、改变多少，很难从观察数据里干净地识别出来。
- CGE / 一般均衡模型：能推演宏观效应，但行为人高度同质、异质性不足，也装不下农户的心理账户、风险态度和家庭约束。

### 1.2 平台给了什么

AgentSociety² 是清华的 LLM 智能体社会模拟平台，提供三类能力：

- 社会人智能体：带情感、需求、动机与认知，行为由大模型实时决策，而不是固定规则表。
- 可干预的社会环境：政策冲击以工具形式暴露给智能体，可以精确控制施加时点与强度。
- 回放与统计分析：每步状态自动落库（SQLite/DuckDB），微观行为到宏观涌现的链条可追溯、可复现。

对农户行为这类强异质性、强情境依赖的决策问题，这套组合比传统 ABM 更能还原"人在具体处境里怎么想"。

### 1.3 为什么传统方法不够，而 LLM 智能体社会有机会

传统方法的两条路各有天花板：

- 事后计量：政策已经落地，只能观测已发生的结果，做不了反事实对照。农户是否改变种植结构、改变多少，很难从观察数据里干净地识别出来。
- CGE / 一般均衡模型：能推演宏观效应，但行为人高度同质、异质性不足，也装不下农户的心理账户、风险态度和家庭约束。

LLM 智能体社会的思路不同：把"农户"做成带人格、记忆与偏好约束的智能体，放在一个可干预的虚拟村庄里，政策作为显式冲击反复施加，微观决策到宏观涌现全链路可回放、可复现、可审计。这套范式对"强异质性、强情境依赖"的农户决策问题，比固定规则 ABM 更能还原人在具体处境里的考量，也天然支持反事实实验——这正是本课题选择它的原因。

## 2. 研究问题与假设

**研究问题**

- RQ1：生产性补贴、收入性补贴、保险补贴、土地流转激励这几类工具，分别如何影响农户的种植结构、要素投入与"务农/务工"劳动力配置？
- RQ2：政策通过哪些微观机制起作用（风险预期、收益比较、流动性约束）？对不同经营规模、年龄、风险偏好的农户，效应是否不同？
- RQ3：政策组合能否在粮食安全（播种面积）与农户增收之间取得平衡？工具之间存在哪些权衡，财政成本分别是多少？

**可检验假设**

- H1：提高粮食直补强度会提高粮食播种面积占比，对"粮经比"敏感的小农更明显。
- H2：保费补贴降低风险规避农户的保守种植倾向，提高高价值、高波动作物与规模经营意愿。
- H3：流转激励促进土地向种粮大户集中，提高规模化与单产，但可能短期加剧小农离农。
- H4：单纯增收工具可能挤压粮食面积，"补贴+保险+流转"组合才可能兼顾安全与增收。

## 3. 实验设计

### 3.1 农户智能体（FarmerAgent）

采用平台官方"行为实验"模式（AgentBase + 手写 step + ask_env 模板提交）：继承 AgentBase，手写 step，
每步「观察 → LLM 决策 → 模板模式提交环境工具」，不启用通用认知循环，LLM 调用少、行为可控：

- 画像（profile）：区域（华北/东北/长江中游/西南）、经营规模（小农<10 亩 / 中农 10-50 / 大户>50）、
  风险偏好、初始资产、家庭兼业收入、年龄、教育、可用作物。
- 每季流程：`observe_market` 获取价格/补贴/保险/天气 → LLM 依画像与观察输出
  `{"plan": {作物: 亩}, "insured": {作物: 亩}, "transfer_in": 亩, "transfer_out": 亩}` →
  通过 `ask_env`（模板模式）提交 `decide_planting` / `buy_insurance` / `transfer_land`。
- 决策历史保留在智能体内（decision_history），用于机制分析。

### 3.2 村庄农业经济环境（AgriPolicyEnv）

基于平台 EnvBase 实现，结构如下：

- 工具面：`observe_market`（价格/补贴/保险/天气）、`decide_planting`、`buy_insurance`、`transfer_land`（租入/转出）、`report_status`（复盘上一季）。
- 政策干预工具：`set_subsidy`、`set_insurance_subsidy_rate`、`set_land_transfer_out_subsidy`，可直接编程调用，也可经平台 `intervene` 以自然语言触发。
- 每步（一个生产季）更新价格与天气冲击，逐户核算收支，写入回放表（`agri_policy_agent_state` / `agri_policy_env_state`），分析阶段直接读 SQLite。

**标定参数独立成文件**：作物目录、单产、价格、成本、保费、租金、灾害阈值全部集中在 `configs/economics.json`，由 `agri_sandbox/economics.py` 加载，不改代码即可替换参数。核算公式是纯函数（零平台依赖，有单元测试），答辩时可以直接逐项讲清楚每一行账怎么算。

### 3.3 政策情景与反事实

| 情景 | 政策设置 |
| --- | --- |
| baseline | 无新增补贴（反事实对照） |
| grain_subsidy | 直补提高到 120 元/亩（对照：不提高） |
| insurance_subsidy | 政府承担 80% 保费 |
| land_transfer_subsidy | 转出补贴 200 元/亩 |
| combined | 上述组合 |

识别策略是分阶段反事实：同一批农户先跑 baseline_steps 个季度（空政策），随后施加情景政策再跑 policy_steps 个季度。前后两阶段的差异就是处理效应估计，农户个体异质性被同一批样本控制住。每个情景重复多次（不同随机种子），估计效应分布与置信区间。

### 3.4 代码结构与运行

```
agri_sandbox/
  economics.py         # 纯经济核算模型（零平台依赖，可单测、可标定）
  profiles.py          # 异质农户画像生成
  farmer_agent.py      # FarmerAgent（AgentBase 子类，手写 step + ask_env）
  agri_policy_env.py   # AgriPolicyEnv（EnvBase 子类，工具 + 回放）
  experiment.py        # ExperimentSpec + build_world + run_one（编排核心）
  run_experiment.py    # CLI 入口（薄层）
  analyze.py           # 回放 SQLite 分析：均值、处理效应、农户级净收入效应
  llm_config.py        # .env 加载与 LLM 配置校验
  patches.py           # 上游兼容补丁（json_repair.dumps / Windows fsync / slice_text_page）
configs/
  policy_scenarios.json   # 情景定义（默认 50 农户、8+8 季、90 天/季、3 次重复）
  economics.json          # 标定参数（作物/价格/成本/保费/租金/灾害阈值）
tests/test_economics.py   # 核算纯函数单测（10 例）
```

运行：

```bash
pip install -r requirements.txt          # agentsociety2>=2.1.5 等
cp .env.example .env                      # 填入 AGENTSOCIETY_LLM_API_KEY 等
python -m agri_sandbox.run_experiment --all
python -m agri_sandbox.analyze
```

已知环境注意：agentsociety2 依赖较重，若本机 pip 装全量依赖遇到冲突，可用 `--no-deps` 安装后再逐个补缺（项目在 llm_config.py 内置了对 json_repair.dumps 的兼容补丁）。

## 4. 数据采集与分析方法

### 4.1 数据源

- `agri_policy_agent_state`：逐季逐户的种植面积、投保面积、流转、毛收入、补贴、赔付、成本、净收入。
- `agri_policy_env_state`：逐季全村均值、投保覆盖率、补贴总额、天气冲击。
- 农户工作区决策日志与对话记录：用于机制质性分析。

### 4.2 指标

- 结果指标：粮食播种面积占比、农户人均纯收入、务农劳动力占比、土地流转率、保险参与率。
- 分配指标：收入基尼系数（按规模分组）。
- 成本指标：单位增收的财政成本（补贴总额/增收额）。
- 权衡：粮食安全指数对增收指数的 Pareto 前沿。

### 4.3 分析

- 情景均值对比 + bootstrap 95% 置信区间。
- 按规模/年龄/风险偏好分群的异质性比较。
- 机制检验：对智能体决策日志做质性编码，看"风险预期路径"与"收益比较路径"是否出现。
- 可复现：固定种子、存档 run_dir、记录模型版本与温度。

## 5. 对评审标准的回应

| 评审维度 | 本研究的安排 |
| --- | --- |
| 社会科学价值 | 直面粮食安全与农民增收的政策议程，输出可操作的事前评估工具 |
| 研究问题创新性 | 用可执行社会模拟做政策沙盒，区别于事后计量与 CGE |
| 方法设计严谨性 | 反事实情景对照 + 重复实验 + 异质性 + 机制检验 + 全链路可复现 |
| 科学发现与理论贡献 | 揭示政策工具权衡与异质性机制，形成机制性发现 |
| 研究影响与拓展潜力 | 参数可标定，可扩展到其他涉农政策与地区，产出可复用实验基础设施 |

## 6. 时间表

| 阶段 | 时间 | 任务 |
| --- | --- | --- |
| 定题与组队 | 8/20-8/31 | 理论框架定稿；组队（农经+建模+数据）；平台账号与 API 额度申请 |
| 原型跑通 | 9/1-9/14 | FarmerAgent + AgriPolicyEnv 跑通基线（**初筛 9/15 前提交**） |
| 实验与写作 | 9/16-9/30 | 全情景实验 + 数据分析 + 论文初稿（答疑 9/17） |
| 迭代完善 | 10/1-10/17 | 机制分析、稳健性检验、论文打磨 |
| 演示准备 | 10/18-10/24 | 海报/交互演示（replay 可视化） |
| 现场评审 | 10/25 | 答辩 |

## 7. 风险与边界

- 外部效度：模拟是受控实验，不等于真实政策效果。结论定位为"机制性启示"而不是"因果估计"。
- 参数示意性：当前 economics.json 是示意值，正式实验需要按公开统计年鉴与农户调查数据标定，报告中要写明参数来源与敏感性。
- 额度约束：200 元 API 额度有限，先小样本（50 农户、少量步数）跑通链路，再放大；模型档位用 NANO 档做高频决策。
- 刻板印象：农户画像基于统计分布生成，不代表任何具体个体；论文中声明抽样逻辑。
- 学术诚信：代码与工作区随作品提交，明确说明 AI 在流程中的角色。

## 8. 下一步

1. 跑通 baseline 连通性自检（3 农户 × 1+1 季已通过）。
2. 50 农户 × 8+8 季 × 3 重复正式实验，产出 runs_index.json 与回放库。
3. 运行 analyze 输出处理效应摘要。
4. 用公开统计年鉴与农户调查数据标定参数，替换示意值。
5. 补机制质性分析，写论文。
